# データモデル仕様

現状、サーバー側のデータモデルは存在しません。

- タイマー状態・設定・進捗はlocalStorage（クライアント側）で管理：
    - `pomodoro_state`：タイマーの状態（分・秒・作業/休憩・セット数・完了回数・累計集中時間・稼働中フラグ・日付）
    - `pomodoro_settings`：作業時間・休憩時間の設定

今後サーバー保存や履歴管理を実装する場合は、本ドキュメントを拡張してください。