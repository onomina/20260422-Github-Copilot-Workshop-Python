# APIリファレンス

このドキュメントは、現在の実装に基づくREST APIエンドポイントとそのリクエスト/レスポンス仕様を記載します。

## 実装状況

現状、Flaskサーバーには `/` のみが実装されており、APIエンドポイントはありません。

- `GET /` : メイン画面（index.html）を返却

今後、設定保存や履歴取得APIが追加された場合は本ドキュメントを更新してください。